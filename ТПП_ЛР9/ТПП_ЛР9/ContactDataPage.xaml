<Page x:Class="ТПП_ЛР9.ContactDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ТПП_ЛР9"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Контактные данные">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Электронная почта:"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Телефон:"/>
        <TextBox x:Name="PhoneTextBox" Grid.Row="3" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BackButton" Content="Назад" Width="100" Height="30" Margin="0,0,10,0" Click="BackButton_Click"/>
            <Button x:Name="NextButton" Content="Далее" Width="100" Height="30" Click="NextButton_Click"/>
        </StackPanel>
    </Grid>
</Page>